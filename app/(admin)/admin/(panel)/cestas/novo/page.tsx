/**
 * Criação de template — formulário inclui campos do template + array de slots.
 * Após criar, o admin é redirecionado para a edição (onde pode gerenciar slots
 * individualmente).
 */

import { CreateTemplateForm } from "./form";

export default function NovaCestaPage() {
  return <CreateTemplateForm />;
}
